language: node_js
node_js:
  - 13.5.0

cache: npm # we can take this off sometimes to see if it builds without the cache

branches:
  only:
    - master
    - dev


script:
  - cd client && npm install
  - npm run build
  - cd ../server && npm install


# deploy:
#   -
# we will eventually have a deploy hook so that it pushes everything to production
# if: branch = master # only want to deploy if we are pushing to master and not dev

